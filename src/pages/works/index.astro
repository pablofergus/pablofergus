---
import { getCollection } from 'astro:content';
import Layout from '@layouts/Default.astro';
import WorkList from '@components/works/WorkList.astro';
const posts = await getCollection('works').then((collection) =>
  collection.reverse()
);
---

<Layout
  title='Works'
  description='Brutal Blog | On this page you can find a collection of blogposts'
  pageTitle='Brutal Blog'
>
  <main class='bg-green p-6'>
    <WorkList posts={posts} />
  </main>
</Layout>
